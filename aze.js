1-


recipeList = filterBySearch(recipeList);
(...)
function filterBySearch(recipeList)
{
	const r = [];
	
	for(toutes les recettes)
	{
		if(recette correspond)
		{
			r.push(recette);
		}
	}
	return r;
}
2-


filterBySearch(recipeList);
(...)
function filterBySearch(recipeList)
{
	for(let i = recipeList.length - 1; i >= 0; i--)
	{
		if(recette ne correspond pas)
		{
			recipeList.splice(i, 1);
		}
	}
}
i = 5
t[0, 1, 2, 3, 4, 5, 6, ]
i++ -> 7